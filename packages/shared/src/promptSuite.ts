/** Semantic versions (YYYY-MM-DD) of each LLM template used on a run. */
export interface PromptSuiteVersions {
  quoteScrape: string;
  chartScrape: string;
  newsScrape: string;
  aiInsights: string;
  aiPrediction: string;
}
