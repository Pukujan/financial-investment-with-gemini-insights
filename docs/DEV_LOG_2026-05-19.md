# Dev log — 19 May 2026

## Prompt registry (`@investai/prompts`)

- New workspace package: versioned templates for quote, chart, news, AI insights, and prediction prompts.
- Agents and `aiService` resolve prompts via `resolve*Prompt()` instead of inline strings.
- `GET /api/agent-scrape/prompts` exposes catalog + `PROMPT_LATEST` for tooling and demos.

## Quote scrape v2 (`2026-05-19`)

- Wired `promptVersion` from prompt eval into `scrapeQuotesWithAgent`.
- Experiments store `promptSuite` with resolved `quoteScrape` version.
- Stronger golden EOD + RAG instructions; model echoes `promptVersion` in JSON.

## Chart scrape v2 + RAG on production jobs

- Chart templates v2 accept per-symbol RAG context.
- Agent jobs set `promptSuite` on create; chart steps pass RAG when `AGENT_SCRAPE_RAG` is true (default).
- RAG block formatting moved to `@investai/prompts/formatRagContext`.

## Agent chart stale UX (from prior session)

- 12h fresh / 7d stale for agent charts; stale shows banner + recommends fresh scrape (no auto job).

## Docs

- [PROMPT_ENGINEERING.md](./PROMPT_ENGINEERING.md) — study guide
- README, AGENTS.md, AGENT_EVALS.md updated for prompt versioning narrative
